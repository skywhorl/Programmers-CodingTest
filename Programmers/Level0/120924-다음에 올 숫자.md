# 다음에 올 숫자
### [Lv.0 - 120924](https://school.programmers.co.kr/learn/courses/30/lessons/120924)

## 문제
등차수열 혹은 등비수열 `common`이 매개변수로 주어질 때, 마지막 원소 다음으로 올 숫자를 return 하도록 solution 함수를 완성해보세요.
## 풀이
- 문제에서 등차/등비인 경우만 존재하기에 구하기 편한 등차수열을 비교한다.
- 공차 또는 등비를 구했으면 마지막 원소의 값에다 적용한다.

``` C#
using System;

public class Solution {
    public int solution(int[] common) {
        // 등차수열일 경우
        if (common[1] - common[0] == common[2] - common[1]) 
        {
            // 공차로 다음 원소 구하기
            return common[common.Length - 1] + common[1] - common[0];
        }
        
        // 문제 상 이외의 경우는 등비수열
        else 
        {
            // 등비로 다음 원소 구하기
            return common[common.Length - 1] * common[1] / common[0];
        }
    }
}
```
